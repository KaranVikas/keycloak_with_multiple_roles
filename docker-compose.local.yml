volumes:
  keycloak_with_multiple_roles_local_postgres_data: {}
  keycloak_with_multiple_roles_local_postgres_data_backups: {}
  

services:
  django:
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: keycloak_with_multiple_roles_local_django
    container_name: keycloak_with_multiple_roles_local_django
    depends_on:
      - postgres
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: keycloak_with_multiple_roles_production_postgres
    container_name: keycloak_with_multiple_roles_local_postgres
    volumes:
      - keycloak_with_multiple_roles_local_postgres_data:/var/lib/postgresql/data
      - keycloak_with_multiple_roles_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres
